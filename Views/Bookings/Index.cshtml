@* @model Booking.web.Models.UserBookingHistory

<style>
    body {
        font-family: Arial, sans-serif;
        background: #f5f7fa;
        margin: 0;
        padding: 0;
    }

    .page-wrapper {
        display: flex;
        margin-top: 80px;
    }

    .sidebar {
        width: 240px;
        background: #ffffff;
        padding: 20px;
        border-right: 1px solid #ddd;
    }

        .sidebar h2 {
            margin-bottom: 20px;
            font-size: 18px;
        }

    .filter-group {
        margin-bottom: 20px;
    }

        .filter-group label {
            display: block;
            margin-bottom: 8px;
            font-weight: bold;
        }

    .btn-filter {
        width: 100%;
        padding: 10px;
        background: #0078ff;
        color: white;
        border: none;
        border-radius: 6px;
        cursor: pointer;
        font-size: 15px;
    }

    .content {
        flex: 1;
        padding: 30px;
    }

    .booking-card {
        background: white;
        border-radius: 12px;
        padding: 20px;
        margin-bottom: 25px;
        box-shadow: 0 2px 6px rgba(0,0,0,0.1);
    }

    .card-header {
        display: flex;
        justify-content: space-between;
        margin-bottom: 15px;
    }

    .badge {
        padding: 6px 12px;
        border-radius: 20px;
        font-size: 13px;
        font-weight: bold;
        color: white;
    }

    .flight {
        background: #0078ff;
    }

    .housing {
        background: #28a745;
    }

    .status {
        padding: 6px 12px;
        border-radius: 20px;
        font-size: 13px;
        font-weight: bold;
    }

    .confirmed {
        background: #d4edda;
        color: #155724;
    }

    .cancelled {
        background: #f8d7da;
        color: #721c24;
    }

    .card-title {
        font-size: 22px;
        font-weight: bold;
        margin-bottom: 10px;
    }

    .dates {
        margin-bottom: 10px;
        color: #555;
    }

    .price {
        font-size: 20px;
        font-weight: bold;
        color: #333;
        margin-bottom: 15px;
    }

    .card-footer button {
        padding: 8px 14px;
        margin-right: 10px;
        border: none;
        border-radius: 6px;
        cursor: pointer;
    }

    .btn-details {
        background: #0078ff;
        color: white;
    }

    .btn-invoice {
        background: #6c757d;
        color: white;
    }

    .btn-cancel {
        background: #dc3545;
        color: white;
    }
</style>

<div class="page-wrapper">

    <!-- Sidebar -->
    <div class="sidebar">
        <h2>Filtrar histórico</h2>

        <div class="filter-group">
            <label>Tipo</label>
            <input type="checkbox"> Voos<br>
            <input type="checkbox"> Alojamentos
        </div>

        <div class="filter-group">
            <label>Estado</label>
            <input type="checkbox"> Confirmado<br>
            <input type="checkbox"> Cancelado<br>
            <input type="checkbox"> Ativo / Inativo
        </div>

        <button class="btn-filter">Aplicar filtros</button>
    </div>

    <!-- Main Content -->
    <div class="content">

        <!-- ===================== -->
        <!-- SECTION: FLIGHT CARDS -->
        <!-- ===================== -->
        @if (Model.Flights != null)
        {
            foreach (var f in Model.Flights)
            {
                <div class="booking-card">

                    <div class="card-header">
                        <span class="badge flight">✈️ Flight</span>
                        <span class="status confirmed">@f.</span>
                    </div>

                    <div class="card-title">@f.OriginCity → @f.DestinationCity</div>

                    <div class="dates">
                        <strong>Departure:</strong> @f.DepartureTime.ToString("dd MMM yyyy") <br />
                        <strong>Arrival:</strong> @f.ArrivalTime.ToString("dd MMM yyyy") <br />
                    </div>

                    <div class="price">€@f.Price</div>

                    <div class="card-footer">
                        <button class="btn-details">Ver detalhes</button>
                        <button class="btn-invoice">Fatura</button>
                    </div>

                </div>
            }
        }

        <!-- ======================= -->
        <!-- SECTION: HOUSING CARDS -->
        <!-- ======================= -->
        @if (Model.Housings != null)
        {
            foreach (var h in Model.Housings)
            {
                <div class="booking-card">

                    <div class="card-header">
                        <span class="badge housing">🏨 Housing</span>
                        <span class="status confirmed">@h.Status</span>
                    </div>

                    <div class="card-title">@h.HousingName</div>

                    <div class="dates">
                        <strong>Check-in:</strong> @h.CheckInDate.ToString("dd MMM yyyy") <br />
                        <strong>Check-out:</strong> @h.CheckOutDate.ToString("dd MMM yyyy") <br />
                    </div>

                    <div class="price">€@h.TotalPrice</div>

                    <div class="card-footer">
                        <button class="btn-details">Ver detalhes</button>
                        <button class="btn-invoice">Fatura</button>
                    </div>

                </div>
            }
        }

    </div>

</div>
 *@